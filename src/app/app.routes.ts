import { Routes } from '@angular/router';
import { MaterialComponent } from '@presentation/pages/material/features/material-list/material';

export const routes: Routes = [
    { path: 'materiales', component: MaterialComponent }
];
